没问题！如果你不想直接覆盖整个文件，只需要在你目前的 `GeometricController.cpp` 文件中的 `compute_and_publish_control()` 函数里，修改以下 **3 个核心地方** 即可。

请找到 `compute_and_publish_control()` 函数，按顺序进行以下替换：

### 修改 1：定义空气阻力矩阵 $\mathbf{D}$

在物理参数定义区域，找到定义惯性张量 `J` 的地方，在它**后面直接加上**阻力矩阵 `D` 的定义：

**新增以下代码：**

```cpp
        // 惯性张量 J
        Eigen::Matrix3d J;
        J << 0.016, 0,     0,
             0,     0.016, 0,
             0,     0,     0.021;

        // ====== 【修改 1：新增阻力矩阵】 ======
        // 空气动力学线性阻力矩阵 D (基于文献辨识结果)
        Eigen::Matrix3d D;
        D << 0.5, 0.0, 0.0,
             0.0, 0.5, 0.0,
             0.0, 0.0, 0.0;
        // =====================================

```

---

### 修改 2：在位置环加入阻力补偿推力 ($F_{drag}$)

向下滚动到“**3. 位置环：计算期望推力**”的部分。你需要计算出当前的空气阻力，并让无人机额外输出推力来抵消它。

**将原来的这行代码：**

```cpp
        // 计算期望的机体受力 F_des (向上，即 -Z 方向)
        Eigen::Vector3d F_des = -K_p * e_p - K_v * e_v + m * a_d - Eigen::Vector3d(0, 0, m * g);

```

**替换为：**

```cpp
        // ====== 【修改 2：加入阻力补偿推力】 ======
        // 利用实际速度 v 和姿态 R 计算世界坐标系下的阻力向量
        Eigen::Vector3d F_drag = m * R * D * R.transpose() * v;

        // 计算期望机体受力，加上 F_drag 以主动抵消空气阻力带来的减速
        Eigen::Vector3d F_des = -K_p * e_p - K_v * e_v + m * a_d - Eigen::Vector3d(0, 0, m * g) + F_drag;
        // ========================================

```

---

### 修改 3：升级微分平坦性公式（解算新的 $\omega_d$）

继续向下滚动到“**5. 微分平坦性：计算期望角速度 omega_d**”的部分。这是最核心的数学修改，需要把原来的推力求导公式替换为包含阻力项的严谨公式。

**将原来的这几行代码：**

```cpp
        // 前馈推力向量的导数 T_dot 约等于 -m * j_d (忽略误差导数) 
        Eigen::Vector3d T_dot = -m * j_d;
        
        // 解析 Roll 和 Pitch 的期望角速度
        double omega_d_x = -(1.0 / T_norm) * y_b_d.dot(T_dot);
        double omega_d_y =  (1.0 / T_norm) * x_b_d.dot(T_dot);

```

**替换为：**

```cpp
        // ====== 【修改 3：升级前馈角速度解算】 ======
        // 依据 Faessler 文献，前馈力变化率需补偿加速度带来的阻力增量
        Eigen::Vector3d T_dot_ff = -m * (j_d + R_d * D * R_d.transpose() * a_d);
        
        double d_x = D(0,0);
        double d_y = D(1,1);
        double d_z = D(2,2);

        // 依据文献方程 (17) 和 (18)，阻力项会略微改变推力归一化常数 (分母)
        double denom_x = T_norm - m * (d_z - d_y) * z_b_d.dot(v);
        double denom_y = T_norm - m * (d_z - d_x) * z_b_d.dot(v);
        
        // 解析 Roll 和 Pitch 的期望角速度 (带阻力补偿)
        double omega_d_x = -(1.0 / denom_x) * y_b_d.dot(T_dot_ff);
        double omega_d_y =  (1.0 / denom_y) * x_b_d.dot(T_dot_ff);
        // ==========================================

```

*(注：紧接着这几行后面的 `omega_d_z` 的计算公式保持不变，无需修改。)*

---

### 总结

只需要改动这三个地方：**加矩阵、加补偿力、改分母和求导公式**。改完之后重新 `colcon build`，你的 X500 就彻底克服了高速转弯时的空气阻力带来的轨迹缩水问题！