这是一个非常经典且极具代表性的自动控制理论问题！你现在遇到的情况完美展现了理论与实际工程之间的差异。

我们先来破除一个关于数据的误解，然后再深入剖析稳态误差的物理根源，最后给出终极的修改代码。

### 🔍 疑问一：为什么位置误差有 3 米，但 `position_variance[2]` 却几乎为 0？

这是一个概念混淆：

* **`position_variance[2]` (位置方差)**：这是 PX4 内部的 EKF（扩展卡尔曼滤波）状态估计器输出的数值。它代表的是系统对自己所处位置的**自信程度（估计不确定度）**。方差为 0，意味着无人机“非常确信自己目前就在 2 米的高度”，没有测量噪声。
* **位置误差 (Tracking Error)**：这是期望位置（5米）和实际位置（2米）的差值。

**结论**：无人机的传感器和状态估计非常完美，它清楚地知道自己离目标还差 3 米。问题出在**控制器的输出力气不够**。

---

### 🔍 疑问二：为什么会产生高达 3 米的稳态误差（Steady-State Error）？

在你的 `GeometricController.cpp` 中，推力的前馈计算完全依赖于这两个物理常数：

```cpp
const double m = 1.535; // kg
const double max_thrust = 32.0; // N

```

* **控制器的幻觉**：控制器以为飞机的最大推力是 32N，悬停所需的重力是 $1.535 \times 9.81 \approx 15N$。因此控制器认为只要输出 **47%** 的油门（$15/32$）就能完美悬停。
* **物理世界的现实**：在 Gazebo 的 `gz_x500` 模型中，四个电机的实际总最大推力通常在 **22N 到 24N** 左右。所以 47% 的油门根本无法提供 15N 的升力，飞机往下掉！
* **稳态误差的形成**：飞机往下掉，位置误差 $e_p$ 开始增大。位置环的比例项 `-K_p * e_p` 开始发力。由于你设置的 Z 轴 $K_p$ 是 2.5，为了弥补缺失的升力差（大约需要补足 5~8N），误差 $e_p$ 被迫拉大到了 2~3 米，达到了一个“残疾的力学平衡”。

---

### 🛠️ 终极解决办法：引入积分项 (I-Term) 构建完整 PID 控制

在纯粹的几何控制（PD控制）中，一旦物理模型（如质量、最大推力、电压下降）不准确，必定产生稳态误差。要消除一切稳态误差，工业界的唯一杀手锏是：**加入积分项（I）**。

请打开你的 **`GeometricController.cpp`** 文件，进行以下 **3 处修改**：

#### 1. 新增积分状态变量

在 `GeometricController` 类的 `private:` 区域（可以在 `uint64_t offboard_setpoint_counter_ = 0;` 的下方），添加一个积分累加器：

```cpp
    bool has_setpoint_ = false;
    uint64_t offboard_setpoint_counter_ = 0;

    // ====== 【新增：Z 轴积分误差累加器】 ======
    double integral_error_z_ = 0.0;

```

#### 2. 修正物理参数与提高 Z 轴刚度

在 `compute_and_publish_control()` 的开头，把 `max_thrust` 改得更接近真实值，同时稍微强化一下 Z 轴的 $K_p$，并定义积分增益 $K_i$：

```cpp
    void compute_and_publish_control()
    {
        // ================= 物理参数 =================
        const double m = 1.535; // kg
        const double g = 9.81;  // m/s^2
        // 【修改：将 32.0 降至 24.0，更贴合 X500 的真实动力边界】
        const double max_thrust = 24.0; // N 
        const Eigen::Vector3d max_torque(0.5, 0.5, 0.5); // Nm
        
        // 惯性张量 J (省略) ...
        // 阻力矩阵 D (省略) ...

        // ================= 进阶控制增益 =================
        // 【修改：提高 Z 轴刚度至 4.0，增强抗掉高能力】
        Eigen::Matrix3d K_p = Eigen::Vector3d(2.5, 2.5, 4.0).asDiagonal();
        Eigen::Matrix3d K_v = Eigen::Vector3d(1.5, 1.5, 1.5).asDiagonal();
        
        // 【新增：Z 轴的积分增益 K_i】
        double K_i_z = 2.0; 

        // 姿态环增益保持不变
        Eigen::Matrix3d K_R = Eigen::Vector3d(0.6, 0.6, 3.0).asDiagonal();
        Eigen::Matrix3d K_w = Eigen::Vector3d(0.08, 0.08, 0.14).asDiagonal();

```

#### 3. 将积分项注入推力计算

在计算 $F_{des}$ 的位置，加入积分运算与抗积分饱和（Anti-windup）逻辑：

**将原来的这部分：**

```cpp
        // ================= 3. 位置环：计算期望推力 =================
        Eigen::Vector3d e_p = p - p_d;
        Eigen::Vector3d e_v = v - v_d;
        
        Eigen::Vector3d F_drag = m * R * D * R.transpose() * v_d;
        Eigen::Vector3d F_des = -K_p * e_p - K_v * e_v + m * a_d - Eigen::Vector3d(0, 0, m * g) + F_drag;

```

**替换为：**

```cpp
        // ================= 3. 位置环：含积分与阻力补偿的期望推力 =================
        Eigen::Vector3d e_p = p - p_d;
        Eigen::Vector3d e_v = v - v_d;
        
        // 【新增：累加 Z 轴积分误差，步长 dt = 0.01s (100Hz)】
        integral_error_z_ += e_p.z() * 0.01;
        // 【抗积分饱和 (Anti-windup)】：防止起飞或坠机时积分项无限膨胀导致失控
        integral_error_z_ = std::clamp(integral_error_z_, -3.0, 3.0); 

        // 阻力补偿向量
        Eigen::Vector3d F_drag = m * R * D * R.transpose() * v_d;
        
        // 计算期望机体受力，加上 F_drag 和 积分补偿项
        Eigen::Vector3d F_des = -K_p * e_p - K_v * e_v + m * a_d - Eigen::Vector3d(0, 0, m * g) + F_drag;
        
        // 【新增：将积分作用力注入到 Z 轴】
        F_des.z() -= K_i_z * integral_error_z_;

```

---

### 修改后的预期效果

保存并重新 `colcon build` 后再次运行。

1. **修正前**：由于模型推力偏差，飞机只能飞到 2m。
2. **修正后（加入积分）**：你会肉眼可见地发现，飞机在到达 2m 后，积分器 `integral_error_z_` 开始迅速累积误差。这个累积的数值会作为额外的推力强制推着飞机继续向上爬升，直到误差缩小为 0（完美到达 5m 高度）。稳态误差将被**数学意义上彻底清零**！