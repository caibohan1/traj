# 文件路径: src/【功能包】/config/params.yaml

controller_node:
  ros__parameters:
    # 这里定义你的所有控制参数及其初始值
  # 位置比例增益 (K_p)
      K_p: [6.0, 6.5, 8.0]
      
      # 速度比例增益 (K_v)
      K_v: [2.0, 2.0, 4.0]
      
      # 位置积分增益 (K_i_p)
      K_i_p: [0.2, 0.45, 2.0]
      
      # 速度积分增益 (K_i_v)
      K_i_v: [0.5, 0.5, 2.0]